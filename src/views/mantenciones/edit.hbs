<div class="container p-4"></div>
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/mantenciones/edit/{{mantencion.id}}" method="POST">
                    <div class="form group">
                        <textarea value="{{mantencion.description}}" name="description" rows="2" class="form-control" placeholder="Ingrese mantenimiento">{{mantencion.description}}</textarea>
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block">
                            Guardar
                        </button>
                    </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>